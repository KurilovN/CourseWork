@model IEnumerable<CourseWork.Models.TypesWaterMeters>
@using System.Data.Entity;
@using CourseWork.Models;
@{
    ViewBag.Title = "Типы счетчиков";
}

<h2>Типы</h2>

<p>
    @Html.ActionLink("Добавить новый", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NameType)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NameType)
            </td>
            <td>
                @Html.ActionLink("Редактирование", "Edit", new { id = item.TypeId }) |
                @Html.ActionLink("Удалить", "Delete", new { id = item.TypeId })
            </td>
        </tr>
    }

</table>
<p>
    <div>
        @Html.ActionLink("Назад", "AdminMain", "Main")
    </div>
</p>
@section Profile{
    <li class="dropdown">
        <a class="dropdown-toggle" data-toggle="dropdown">
            @User.Identity.Name
            <span class="caret"></span>
        </a>
        <ul class="dropdown-menu">
            <li>@Html.ActionLink("Выход из профиля", "Logout", "Account")</li>
        </ul>
    </li>
}
@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
    @Scripts.Render("~/bundles/jquerymask")
}


